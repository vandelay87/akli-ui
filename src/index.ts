// Barrel export for @akli-dev/ui.
//
// No components have been extracted from personal-website yet — this file
// is a placeholder so `vite build` has a valid library entry point. Real
// component exports land starting with issue #4 (component extraction) and
// the package's build architecture (exports map, CSS Modules, etc.) is
// settled by the milestone-1 spike in issue #3.
export {}
